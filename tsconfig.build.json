{
  "extends": "./tsconfig.json",
  // 👇️ this may vary depending on how you
  // name your test files
  "exclude": ["src/**/*.spec.ts", "src/**/*.test.ts", "migrate.ts"],
  "compilerOptions": {
    "outDir": "./dist" /* Specify an output folder for all emitted files. */
  }
}
